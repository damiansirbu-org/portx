# PORTX Parameter Conversion Implementation Guide\n\n## Overview\n**Total Tools**: 369\n**Analysis Date**: 2025-09-27 03:46:19\n\n## Implementation Phases\n\n### Phase 1: Critical Tools (Immediate Fix Required)\n\n#### 🔥 CRITICAL (1 tools)\n\n**git (git-extras)**\n- Issue: Embedded path parameters completely break git operations in WSL\n- Convert: --git-dir, --work-tree, --exec-path\n- Never Convert: --grep, --author, --committer\n\n#### ⚡ HIGH - CRITICAL CONFLICT (1 tools)\n\n**rg (ripgrep)**\n- Issue: Pattern vs path conflict - regex patterns must NEVER be converted\n- Convert: --ignore-file, --path-separator\n- Never Convert: --regexp, --file, --type-add\n\n#### ⚡ HIGH - PATTERN CONFLICT (1 tools)\n\n**fd (fd)**\n- Issue: Similar pattern vs path conflict as ripgrep\n- Convert: --base-directory, --search-path\n- Never Convert: --glob, --exclude, --regex\n\n#### ⚡ HIGH (12 tools)\n\n**7za (7zip)**\n- Issue: Archive operations fail without path conversion\n- Convert: -o, --output-dir, --base-dir\n\n**ag (ag)**\n\n**delta (git-extras)**\n\n**gitstatusd (git-extras)**\n\n**tar (git-extras)**\n\n## Implementation Strategy\n\n### 1. Parameter Detection Logic\n```python\ndef should_convert_parameter(tool_name, param_name, param_value):\n    tool_config = CONVERSION_MAPPING['tools'].get(tool_name)\n    if not tool_config:\n        return detect_path_heuristically(param_value)\n    \n    # Check explicit rules\n    if param_name in tool_config['parameter_rules'].get('never_convert', []):\n        return False\n    if param_name in tool_config['parameter_rules'].get('always_convert', []):\n        return True\n    \n    # Check for regex patterns\n    if contains_regex_metacharacters(param_value):\n        return False\n    \n    # Default path detection\n    return is_unix_absolute_path(param_value)\n```\n\n### 2. Output Conversion Logic\n```python\ndef convert_tool_output(tool_name, output_text):\n    tool_config = CONVERSION_MAPPING['tools'].get(tool_name)\n    if not tool_config or not tool_config['output_conversion']['needed']:\n        return output_text\n    \n    # Convert Windows paths to Unix paths in output\n    return re.sub(\n        r'C:\\([^\s:]+)',\n        r'/mnt/c/\1',\n        output_text.replace('\\', '/')\n    )\n```\n\n### 3. Priority Implementation Order\n1. **git.exe**: Fix `--git-dir`/`--work-tree` embedded paths\n2. **ripgrep/fd**: Implement pattern vs path detection\n3. **Archive tools**: Basic path conversion for file operations\n4. **Build tools**: Output path conversion for logs\n5. **Other tools**: Systematic rollout based on priority