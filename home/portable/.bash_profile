# Git Bash Profile - Sources modular bashrc system
# This file is automatically loaded by Git Bash on startup


# Source the main bashrc which loads all modules
if [[ -f "$HOME/.bashrc" ]]; then
    source "$HOME/.bashrc"
fi
