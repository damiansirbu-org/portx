{
  "name": "fastboot",
  "version": "1.0.0",
  "tools": [
    {
      "executable": "fastboot.exe",
      "description": "Android fastboot protocol tool for bootloader flashing and device management",
      "dependencies": [
        "system-windows"
      ],
      "usage": "fastboot devices # List devices in fastboot mode\\nfastboot flash boot boot.img # Flash boot partition\\nfastboot oem unlock # Unlock bootloader\\nfastboot reboot # Reboot device\\nfastboot getvar all # Get device variables"
    }
  ],
  "paths": [
    "./"
  ]
}
