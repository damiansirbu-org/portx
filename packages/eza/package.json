{
  "name": "eza",
  "version": "1.0.0",
  "tools": [
    {
      "executable": "eza.exe",
      "description": "Modern ls replacement with Git awareness, icons, and tree view",
      "dependencies": "windows",
      "usage": "eza -la # Long listing with hidden files\\neza --tree --level=2 # Tree view with depth\\neza --git # Show Git status\\neza --icons --color=always # Colorized with icons"
    }
  ],
  "paths": [
    "./"
  ]
}
