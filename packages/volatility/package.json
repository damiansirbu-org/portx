{
  "name": "volatility",
  "version": "1.0.0",
  "description": "Advanced memory forensics framework for incident response and malware analysis",
  "tools": [
    {
      "executable": "vol.exe",
      "description": "Extract digital artifacts from volatile memory (RAM) dumps for forensic analysis",
      "usage": "vol -f memory.dmp imageinfo # Identify OS and profile\\nvol -f memory.dmp --profile=Win10x64 pslist # List running processes\\nvol -f memory.dmp --profile=Win10x64 netscan # Network connections\\nvol -f memory.dmp --profile=Win10x64 malfind # Find malware artifacts\\nvol -f memory.dmp --profile=Win10x64 hashdump # Extract password hashes",
      "dependencies": "windows"
    }
  ],
  "paths": [
    "./"
  ]
}
