{
  "name": "cloc",
  "version": "2.06",
  "description": "Source line counter with language detection and detailed statistics",
  "tools": [
    {
      "executable": "cloc.exe",
      "description": "Multi-language source code line counter with complexity metrics",
      "usage": "cloc src/ # Count lines in source directory\\ncloc --by-file *.py # Per-file statistics for Python files\\ncloc --diff version1/ version2/ # Compare two code versions\\ncloc --exclude-dir=node_modules,dist . # Exclude specific directories\\ncloc --json --out=stats.json . # Output detailed JSON statistics",
      "dependencies": [
        "perl"
      ]
    }
  ],
  "paths": [
    "./"
  ],
  "tags": ["analysis", "metrics", "perl", "development"]
}