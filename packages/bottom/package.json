{
  "name": "bottom",
  "version": "0.9.6",
  "description": "Cross-platform system monitor with customizable TUI and themes",
  "tools": [
    {
      "executable": "btm.exe",
      "description": "Interactive system monitor with CPU, memory, network, and disk widgets",
      "usage": "btm # Launch full interactive monitor\\nbtm -b # Basic mode without graphs and expanded details\\nbtm -S # Sort processes by memory usage\\nbtm --color=gruvbox # Apply gruvbox color theme\\nbtm -r 2000 # Refresh every 2 seconds instead of default 1s",
      "dependencies": "windows"
    }
  ],
  "paths": [
    "./"
  ],
  "tags": ["monitor", "system", "native", "tui"]
}