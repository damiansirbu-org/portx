{{- if .Values.userService.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  name: user-service-secret
  labels:
    app: user-service
type: Opaque
data:
  db_password: {{ .Values.userService.database.password | b64enc }}
  jwt_secret: {{ .Values.userService.jwt.secret | b64enc }}
{{- end -}}