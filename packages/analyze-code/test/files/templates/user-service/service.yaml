{{- if .Values.userService.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.userService.name }}
  labels:
    app: user-service
spec:
  selector:
    app: user-service
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  type: ClusterIP
{{- end -}}