{
  "name": "kube-linter",
  "version": "1.0.0",
  "tools": [
    {
      "executable": "kube-linter.exe",
      "description": "Static analysis tool for Kubernetes YAML files and security best practices",
      "dependencies": "windows",
      "usage": "kube-linter lint deployment.yaml # Lint single file\\nkube-linter lint k8s/ # Lint directory\\nkube-linter lint --config custom.yaml manifests/ # Custom configuration\\nkube-linter list checks # Show available checks"
    }
  ],
  "paths": [
    "./"
  ]
}
